{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "Attach to uci process",
            "program": "${workspaceFolder}/target/debug/uci",
            "processId": "${command:pickProcess}"
        }
    ]
}